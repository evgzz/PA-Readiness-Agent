# Copyable Codex kickoff

```text
Build PA Readiness from SPEC.md, README.md, root/scoped AGENTS.md, the ADR register,
and docs/codex/BUILD_SEQUENCE.md. Task 00 original contracts are complete.
Start Task 01 and implement pending v2 contract extensions before their use,
keeping deterministic synthetic, read-only components. Preserve existing user work and authoritative evidence.

Keep agent proposals, harness authority, independent evaluation, governance gates,
and reporting separate. Select exactly one runtime loop per run. Implement SDK
model/tool callbacks only through the same metering and authorization boundaries.

Implement the event and metric contracts, including event-specific payload checks,
producer/channel controls, complete required evidence, denominators and unresolved
counts. Keep private labels outside runtime/optimizer access and public telemetry.
Compute gates in governance; reporting only displays source-linked snapshots.

Run meaningful acceptance checks and record actual results and limitations. Update
docs/BUILD_STATUS.json as implementation proceeds. Revise scaffold-only checks
when runtime implementation actually begins; do not leave misleading assertions.

When a real model/checkpoint, endpoint, credential, benchmark right, numeric policy,
named owner or qualification dataset is missing, retain NOT_CONFIGURED/NOT_RUN and
finish the authorized local work. Do not substitute mocks and label them real.
Do not create payer/clinical effects, publish, deploy or send reports without the
separate authorization needed for that action. Report the next concrete inputs.
```

For v1 also read docs/V1_GAP_FIXES.md. Complete the learning workflow from
reviewed real-agent development traces through controlled comparison, preserving
all NOT_RUN statuses until execution. Follow the dependency loop in BUILD_SEQUENCE.md.

For v2 read docs/V2_GAP_FIXES.md and its corresponding-specs matrix. Complete
V2-A–D through actual evidence, including 60-trace human review and cost/upgrade
exercises. V2-Q is separate. Do not confuse the specification version with
schema/package versions or mark new field contracts as implemented.
