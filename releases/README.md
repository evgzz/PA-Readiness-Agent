# Release records

The JSON files are incomplete templates, not computed gate results or approvals.
Fill records only from actual evidence and authenticated decisions. Store private
evidence in controlled storage, with safe references in reviewed public summaries.
Gate computation belongs to `pa_governance`; reporting cannot grant authorization.
