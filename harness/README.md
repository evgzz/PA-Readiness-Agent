# Runtime harness

Package: `pa_harness`. Status: NOT_IMPLEMENTED.
Own scope, authorization, budgets, run supervision, model metering, output
validation, action state, approval consumption and reconciliation. Inject exactly
one loop from runtime_adapters. No eval labels or scoring inside runtime controls.
See [harness specification](../docs/HARNESS.md) and [instrumentation](../docs/INSTRUMENTATION.md).
Planned modules are listed in docs/REPO_MAP.json; the package remains a skeleton.
