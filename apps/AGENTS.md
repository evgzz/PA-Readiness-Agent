# Codex instructions — composition applications

Wire concrete packages here through shared contracts. Maintain process identity,
mount, network and credential boundaries. Report views are read-only projections;
never implement release-gate logic or fabricate approvals in the UI. Use source-
linked metric snapshots, explicit freshness, and dataset-specific access controls.
Read scoped component instructions before modifying their implementations.
