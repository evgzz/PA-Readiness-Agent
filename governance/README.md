# Governance

Package: `pa_governance`. Status: NOT_IMPLEMENTED.

Own finding transitions, verified closure, program metrics, release gates and separate authenticated authorization records. Consume independent result artifacts through contracts; never rewrite them.

Planned files under `src/pa_governance/`: findings.py, program_metrics.py, release_gate.py, approvals.py.
Only this package skeleton and specification are supplied. The planned modules
do not yet exist. Read the root SPEC, architecture, and this folder's AGENTS.md.
